$(function(){
    $('#public-service-accept').addClass('active open');
    $('#user_approval').addClass('active');

    //$('a[data-role=user-approval-unapprove]').on('click', function() {
    //    var $trigger = $(this);
    //    var target = $trigger.data('url');
    //    var id = $trigger.data('id');
    //
    //    if (confirm('确定撤消吗？')) {
    //        $.post(target, function(){
    //            $('tr#' + id).remove();
    //        })
    //    }
    //});

});